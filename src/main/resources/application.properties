spring.application.name=inventory-service

# ?o?n này OK r?i, gi? nguyên
spring.datasource.url=jdbc:postgresql://${DB_HOST:localhost}:5432/${DB_NAME:inventory_service}
spring.datasource.username=${DB_USER}
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true 
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# --- QUAN TR?NG: B?O M?T TRÊN SERVER ---
# N?u ông ?? "*", hacker scan IP server ông th?y c?ng 8080
# Nó vào /actuator/env là nó l?y ???c password DB, bi?n môi tr??ng, secret key...
# Ch? m? nh?ng cái này thôi:
management.endpoints.web.exposure.include=health,info,prometheus,metrics
management.endpoint.health.show-details=always